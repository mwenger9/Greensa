import { ConferenceProvider } from './conference-provider';

describe('ConferenceProvider', () => {
  it('should create an instance', () => {
    expect(new ConferenceProvider()).toBeTruthy();
  });
});
